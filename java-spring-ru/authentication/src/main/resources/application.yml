---

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always

# BEGIN
rsa:
  private-key: classpath:private.pem
  public-key: classpath:public.pem
# END